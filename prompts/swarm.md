---
description: 多子代理 swarm 编排：强制拆分任务、并行派发子代理、汇总交付
argument-hint: "<任务描述>"
---
用户已通过 `/swarm` 明确授权子代理委派，并要求**多代理并行**执行。执行要求：

1. 先用 `read` 加载 swarm skill（在可用技能列表中定位其 SKILL.md 路径），
   严格按其编排手册执行。
2. 你（父代理）禁止独自完成任务本体：第一步必须输出通道计划，把任务拆成
   2~4 条相互独立的子代理通道，然后用一个 `subagent` 调用的
   `workflowScript` + `runs.all([...])` 并行派发（默认 async）。可写通道必须声明不重叠
   的文件/契约边界；需要并行写入时使用独立 `worktree: true`，禁止多个 worker 共用
   一个可写 cwd。
3. 子代理完成后必须通过 `handoffPath`、`artifactPaths` 或 patch 引用交回实际变更。
   父代理要把已接受的变更按顺序整合进自己的当前工作区，再运行最终 LSP、受影响范围
   的构建/测试并检查 diff；子代理 worktree 内的通过结果不能替代父代理对最终工作区的验证。
4. 仅当任务原子不可分时才允许缩减为单子代理，且必须在交付说明中写明理由。
5. 全部通道结束后由你交叉核对、仲裁冲突、整合代码、完成最终验证并交付；lane 失败、
   patch 无法应用或 handoff 不完整时，保留现场并报告阻塞，不能静默换执行模式。

待编排的任务如下：

$@
