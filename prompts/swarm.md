---
description: 多子代理 swarm 编排：分解任务、并行派发子代理、汇总交付
argument-hint: "<任务描述>"
---
用户已通过 `/swarm` 明确授权子代理委派。先用 `read` 加载 swarm skill
（在可用技能列表中定位其 SKILL.md 路径），并严格按其中的编排手册执行：
定界 → 分解通道 → async 派发（冷启动完备的任务包）→ 回收汇总 → 父代理验收交付。

待编排的任务如下：

$@
