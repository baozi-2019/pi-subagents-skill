---
description: 多子代理 swarm 编排：强制拆分任务、并行派发子代理、汇总交付
argument-hint: "<任务描述>"
---
用户已通过 `/swarm` 明确授权子代理委派，并要求**多代理并行**执行。执行要求：

1. 先用 `read` 加载 swarm skill（在可用技能列表中定位其 SKILL.md 路径），
   严格按其编排手册执行。
2. 你（父代理）禁止独自完成任务本体：第一步必须输出通道计划，把任务拆成
   2~4 条相互独立的子代理通道，然后用一个 `subagent` 调用的
   `workflowScript` + `runs.all([...])` 并行派发（默认 async）。
3. 仅当任务原子不可分时才允许缩减为单子代理，且必须在交付说明中写明理由。
4. 全部通道结束后由你交叉核对、仲裁冲突、统一验收并交付。

待编排的任务如下：

$@
